models:
  - pydiggs
    ## custom tests
  - california_custom_arcgis_boreholes
    ## custom tests    
  - inclinometers:
      tests:
        - at_least_one_row
      columns:
        - name: Depth_m
          tests:
            - not_null
